name: news_mobile_app
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.2
  webview_flutter: ^4.2.2
  flutter_secure_storage: ^8.0.0
  local_auth: ^2.1.6
  firebase_messaging: ^14.6.5
  file_picker: ^5.3.0
  dio: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
